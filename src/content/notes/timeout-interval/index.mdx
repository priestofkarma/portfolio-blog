---
title: Скрипти та час їх виконання, setTimeout(), setInterval()
description: Нотатки з курсу JavaScript та React.
path: /notes/timeout-interval
date: 2021-07-20
tags: ["JavaScript"]
---

### setTimeout, setInterval, clearInterval

```jsx
/* через 2000 мс запуститься функція console.log */
const timer = setTimeout(() => {
    console.log("hello!");
}, 2000);

/* кожних 1000 мс запуститься ананомінна функція з console.log('tick!'); */
const timerId = setInterval(() => {
    console.log("tick!");
}, 1000);

/* зупинити виконання таймера */
clearInterval(timerId);
```

### Рекурсивний виклик setTimeout

```jsx
/* через 2000 мс запуститься функція console.log */
let id = setTimeout(function log() {
    console.log("hello!");
    id = setTimeout(log, 500);
}, 500);
```

[JavaScript-анімації](https://learn.javascript.ru/js-animation)

### Анімація за допомогою setInterval

```jsx
function myAnimation() {
    const elem = document.querySelector(".box");
    let pos = 0;

    const id = setInterval(frame, 10);
    function frame() {
        if ((pos = 300)) {
            clearInterval(id);
        } else {
            pos++;
            element.style.top = pos + "px";
            element.style.left = pos + "px";
        }
    }
}

myAnimation();
```
