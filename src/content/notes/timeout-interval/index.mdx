---
title: Скрипты и время их выполнения, setTimeout(), setInterval()
description: Заметки по курсу JavaScript и React.
path: /notes/timeout-interval
date: 2021-07-20
tags: ["JavaScript"]
---

### setTimeout, setInterval, clearInterval

```jsx
/* через 2000 мс запустится функция console.log */
const timer = setTimeout(() => {
    console.log("hello!");
}, 2000);

/* каждых 1000 мс запустится ананоминая функция с console.log('tick!'); */
const timerId = setInterval(() => {
    console.log("tick!");
}, 1000);

/* остановить выполнение таймера */
clearInterval(timerId);
```

### Рекурсивный вызов setTimeout

```jsx
/* через 2000 мс запустится функция console.log */
let id = setTimeout(function log() {
    console.log("hello!");
    id = setTimeout(log, 500);
}, 500);
```

[JavaScript-анимации](https://learn.javascript.ru/js-animation)

### Анимация с помощью setInterval

```jsx
function myAnimation() {
    const elem = document.querySelector(".box");
    let pos = 0;

    const id = setInterval(frame, 10);
    function frame() {
        if ((pos = 300)) {
            clearInterval(id);
        } else {
            pos++;
            element.style.top = pos + "px";
            element.style.left = pos + "px";
        }
    }
}

myAnimation();
```
